fun main(args: Array<String>) {
    /*
    Variables = a little bucket where we can put information
    Types: String, Int, Double, Boolean, Float, Char
    Int: number
    */
    var name = "Shankha"
    var age = 31 // we are implicitly defining age to be an integer
    var myAge:Int = 32 // Explicit way of defining a variable as integer
    println(age)
    println(myAge)
    println("$name is $age years old.")
}